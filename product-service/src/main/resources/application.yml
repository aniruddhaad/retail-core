Server:
  port: 8085
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/retail_core
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info